// Placeholder for Phase 3: detect music, mute segments, replace audio.
// For Phase 1, just export a stub.
export function isCopyrightSafe(segmentPath: string): boolean {
  // TODO: implement in Phase 3 with audio fingerprinting
  return true;
}
